# user-input-validator
A modular C library that validates usernames, email addresses, and passwords using manual string parsing, character validation, and robust error handling without relying on regular expressions or external libraries.
